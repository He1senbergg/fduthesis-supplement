\documentclass[type=master]{fduthesis}

% \usepackage[backend=biber,style=gb7714-2015,doi=false,url=false]{biblatex}
% \addbibresource[glob]{main.bib}

\fdusetup{
  style = {
    footnote-style = xits,
    bib-backend = bibtex,
    bib-style = numerical,
    bib-resource = {main.bib},
  }, 
  info = {
    title = { },
    title* = { },
    % author = { },
    % supervisor = { },
    major = { },
    degree = professional,
    department = { },
    id = { },
    % instructors = {
    %   { },
    %   { },
    % },
    date = { },
    keywords = { },
    keywords* = { },
    clc = { },
  }
}

% 设置页面的行距和字体大小
\usepackage{setspace}
\setlength{\baselineskip}{20pt}  % 设置行距为 20 磅

% 数学公式字体设置为 12 磅
\usepackage{amsmath}
\setlength{\abovedisplayskip}{10pt plus 2pt minus 2pt}
\setlength{\belowdisplayskip}{10pt plus 2pt minus 2pt}

% 需要的宏包可以自行调用
\usepackage{physics}

% 自主额外新加的
\usepackage{subfigure}
\usepackage{graphicx}
\usepackage{changepage}  % 需要引入changepage宏包
\usepackage{hyperref}       % hyperlinks
\usepackage{xcolor} % 确保 xcolor 被加载

\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsfonts}       % blackboard math symbols
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage[dvipsnames]{xcolor}
\usepackage{amstext}
\usepackage{amsmath}
\usepackage{dsfont}
\usepackage{bbm}
\usepackage[capitalize,noabbrev,nameinlink]{cleveref}
\renewcommand{\thefigure}{\thechapter.\arabic{figure}}
\renewcommand{\thetable}{\thechapter.\arabic{table}}
\crefformat{figure}{图~#2#1#3}  % 将 "Figure" 替换为 "图"
\crefformat{table}{表~#2#1#3}    % 将 "Table" 替换为 "表"
\usepackage{multirow}
\usepackage{wrapfig}
\usepackage{float}
\usepackage{enumitem}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{siunitx}
\usepackage{algorithm}
\usepackage{algorithmic}

\usepackage{fancyhdr}  % 引入 fancyhdr 包

% 需要的命令可以自行定义
\newcommand{\hilbertH}{\symcal{H}}
\newcommand{\ee}{\symrm{e}}
\newcommand{\ii}{\symrm{i}}

% 清空所有页眉和页脚设置
\pagestyle{empty}  % 默认全局不显示页眉页脚

\ctexset{%
    chapter={
        number={\chinese{chapter}}, % 将章节编号转换为汉字（例如 1 显示为“一”）
    }
}

\begin{document}

%% 此处为修改超链接（即引用、标签、章节记号等内容）的颜色
% 调整 hyperref 的设置
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    urlcolor=black
}

% 这个命令用来关闭版心底部强制对齐，可以减少不必要的 underfull \vbox 提示，但会影响排版效果
% \raggedbottom

% 前置部分包含目录、中英文摘要以及符号表等
\frontmatter

\begin{abstract}
 
\end{abstract}

\begin{abstract*}

\end{abstract*}

% 目录
% \cleardoublepage
\newpage
\thispagestyle{empty}  % 删除目录页的页眉
% \addtocontents{toc}{\protect\thispagestyle{empty}}
% 不插入页眉，插入页脚（罗马数字页码）
\pagestyle{plain}  % 仅显示页脚
\renewcommand{\thepage}{\roman{page}}  % 使用罗马数字页码
\tableofcontents
% 插图目录
\listoffigures
% 表格目录
\listoftables

% \pagestyle{fancy} % 这里可以恢复页眉样式

\newpage
% 主体部分是论文的核心
\mainmatter

% 建议采用多文件编译的方式
% 比较好的做法是把每一章放进一个单独的 tex 文件里，并在这里用 \include 导入，例如


% 后置部分包含参考文献、声明页（自动生成）等
\backmatter

% 打印参考文献列表
\printbibliography

\begin{acknowledgements}

\end{acknowledgements}

\end{document}
